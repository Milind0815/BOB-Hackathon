# Submission Notes

Keep the top-level template structure intact. Put project implementation inside `src/`. Use synthetic/de-identified data only. Never commit credentials, `.env` files or private patient information.
